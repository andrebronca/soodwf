export class Dog {
    constructor(name) {
        this.name = name;
        this.nickName = "Doggy";
    }
    greet(sentence) {
        console.log(`${sentence} ${this.name} (${this.nickName})`);
    }
}
//# sourceMappingURL=Dog.js.map